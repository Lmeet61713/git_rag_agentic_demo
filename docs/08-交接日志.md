# 交接日志

> 交接时间：2026-08-06
> 交接人：Codex（当前工作会话）  
> 交接对象：下一个接手人或继续维护者  
> 关联文档：[00-文档索引.md](00-文档索引.md)、[04-长任务流.md](04-长任务流.md)、[10-验收标准.md](10-验收标准.md)

## 1. 当前状态

| 项目 | 状态 |
|------|------|
| 后端 FastAPI | 运行中，http://127.0.0.1:8000，/api/health 正常 |
| 前端 Vite | 运行中，http://127.0.0.1:5173/ 可访问 |
| 后端依赖 | 已通过 uv 安装核心依赖 |
| 前端依赖 | 已通过 npm 安装 |
| 测试 | `uv run pytest` 56 项通过、1 项跳过；`uv run ruff check backend` 通过；前端 Vitest 9 项通过；`npm run build` 通过 |
| GitHub OAuth | 真实浏览器登录已验证；回调 host 不一致已修复；登录直接请求 127.0.0.1:8000 |
| 仓库列表与入库 | 7 个仓库已同步；6 个有本地镜像的仓库已用 Chroma + ONNX 重建，当前 4697 条向量记录 |
| 运行环境 | 后端必须能访问 GitHub；受限沙箱内启动会报 `httpx.ConnectError`，需在普通终端启动 |
| 向量检索 | Chroma + 本地 ONNX `bge-small-zh-v1.5` 已启用；SQLite + Hash 仅作兜底；增量更新与图片描述缓存已实现 |
| 模型问答 | DeepSeek 配置为 active，直接调用已验证成功；LLM 结构化选工具 + 规则兜底 |
| 模型保底 | DeepSeek 常驻；调用失败时自动降级到本地 Ollama；均失败时明确提示并返回本地检索兜底 |
| 检索质量 | 项目级技术栈摘要、并集混合检索、置信度过滤已实现；`general_chat`、检索后重排、LLM 相关性校验与多轮重试已补齐，详见 [11-检索能力问题分析.md](11-检索能力问题分析.md) |

## 2. 时间线与任务记录

| 时间 | 任务 | 状态 |
|------|------|------|
| 2026-08-04 上午 | 完善项目要求、项目内容、技术栈、长任务流文档 | 完成 |
| 2026-08-04 上午 | 搭建 FastAPI 后端骨架与配置 | 完成 |
| 2026-08-04 上午 | 实现 GitHub OAuth、仓库、入库、检索、Agent、记忆 API | 完成（代码） |
| 2026-08-04 上午 | 搭建 Vue 3 前端页面 | 完成 |
| 2026-08-04 上午 | 安装后端/前端依赖并运行测试 | 完成 |
| 2026-08-04 10:10 | 前后端服务首次启动 | 完成 |
| 2026-08-04 10:25 | 后端重启并确认 .env OAuth 配置生效 | 完成 |
| 2026-08-04 上午 | 浏览器验证 GitHub 登录回调 | 未通过（invalid_state） |
| 2026-08-04 11:35 | 生成交接日志 | 完成 |
| 2026-08-04 11:55 | 修复 OAuth host 不一致，完成增量入库、概览/读取工具、记忆 CRUD、前端测试 | 完成 |
| 2026-08-04 12:23 | 定位后端沙箱无法访问 GitHub，清理多后端进程并在沙箱外重启；暂停工作流 | 完成 |
| 2026-08-04 12:35 | 修复仓库同步未写 `full_name` 导致 502，预同步 5 个公开仓库 | 完成 |
| 2026-08-04 13:05 | 用户真实浏览器验证登录、仓库列表与 6 个仓库入库；完善向量化文档与验收标准 | 完成 |
| 2026-08-04 20:16 | 完成同步日志、索引摘要（含现有仓库回填）、Agent 历史上下文修复、记忆管理页面与前后端测试 | 完成 |
| 2026-08-04 20:30 | 完成聊天会话管理：会话列表、历史消息、重命名与删除 | 完成 |
| 2026-08-04 20:50 | 完成模型目录下拉与本地 Ollama 接入，明确未配置模型时的兜底提示 | 完成 |
| 2026-08-04 21:00 | 更新 DeepSeek/DashScope 最新模型名，Agent 支持问候与模型身份直接应答，保存后刷新配置 | 完成 |
| 2026-08-04 21:25 | 新增应用咨询与仓库元数据工具，回填仓库真实创建时间，减少无效向量检索 | 完成 |
| 2026-08-04 21:45 | 分析“Vue 架构检索错误”根因，输出改进计划，本轮未改代码 | 完成 |
| 2026-08-06 | 接入 ONNX + Chroma + LangGraph + SSE 逐 token 流式；新增 URL 导入、项目级技术栈摘要、并集混合检索、置信度过滤 | 完成 |
| 2026-08-06 | 前端重做：左侧导航、Markdown 聊天、来源卡片、当前模型快速切换 | 完成 |
| 2026-08-06 | 定位“非仓库闲聊仍触发检索”问题；新增 `project_intro`、非检索意图强制兜底、LLM 结构化选工具 | 部分完成 |
| 2026-08-06 | 区分“模型调用失败”与“未配置模型”，补充 LLM 调用失败日志 | 完成 |
| 2026-08-06 | 新增 `general_chat` 输出越界校验，天气/生活问题即使触发检索也会清空检索证据 | 完成 |
| 2026-08-06 | 完成检索后重排、LLM 相关性校验与多轮重试，新增 validate 节点 | 完成 |
| 2026-08-06 | 后端测试增至 47 项，项目进度汇报 PPT 已生成 | 完成 |
| 2026-08-06 | DeepSeek 常驻 + Ollama 自动保底，前端新增“Ollama 保底”标签 | 完成 |
| 2026-08-06 | 聊天消息持久化 tool/mode，历史会话重新加载后保留标签；长期记忆中文分词召回、项目隔离与会话摘要去重 | 完成 |
| 2026-08-06 | 修复显式项目跨项目污染：无结果重试不再放宽用户明确指定的项目，图片/文档专用检索不降级类型 | 完成 |
| 2026-08-06 | 后端测试增至 56 项；真实数据验证显式项目图片检索、项目概述与记忆中文召回 | 完成 |
| 2026-08-06 | 顶部导航布局、可折叠会话栏、lucide 品牌标识；新增 `repo_brief` 工具、口语化项目介绍与语言级项目摘要检索 | 完成 |

## 3. 已完成任务（按工作流）

| 阶段 | 完成情况 |
|------|----------|
| 阶段 0 项目骨架 | 完成，后端可启动，健康检查通过 |
| 阶段 1 GitHub OAuth | 真实浏览器登录已验证 |
| 阶段 2 仓库列表与入库状态 | 6 个公开仓库已验证 |
| 阶段 3 仓库同步 | 6 个真实仓库已验证，同步日志已记录 |
| 阶段 4 文件解析 | 代码完成，图片描述缓存已实现，图生文真实 API 待验证 |
| 阶段 5 Embedding 与 Chroma | ONNX `bge-small-zh-v1.5` + Chroma 已启用，6 个仓库已重建，当前 4697 条向量 |
| 阶段 6 检索服务 | 项目技术栈摘要、并集混合检索、置信度过滤已实现，Vue 类项目级问题已验证 |
| 阶段 7 Agent | LangGraph 状态流已接入，SSE 逐 token 流式；`general_chat` 输出越界校验、检索后重排、LLM 相关性校验与多轮重试已完成 |
| 阶段 8 记忆系统 | CRUD、前端管理页与聊天会话管理完成，自动化测试通过 |
| 阶段 9 前端 | 登录、仓库、聊天（含会话侧栏）、模型配置、记忆页面完成，已安装依赖 |
| 阶段 10 联调和测试 | 登录与入库已验证，后端 56 项、前端 9 项测试通过；Agent/图片/记忆端到端待验证 |

## 4. 未完成与阻塞项

| 事项 | 说明 | 建议 |
|------|------|------|
| GitHub 回调 invalid_state | 已修复：Vite host 固定 127.0.0.1，localhost 自动跳转，真实浏览器已验证 | 关闭 |
| Vite 5173 残留进程 | 已用 strictPort 固定 5173 | 启动前清理旧进程即可 |
| 后端沙箱无法访问 GitHub | 回调返回 `httpx.ConnectError` | 在普通终端启动后端，不要在受限沙箱内运行 |
| 8000 端口多进程 | 浏览器可能访问旧后端，修复不生效 | 启动前检查 `netstat -ano | findstr :8000`，只保留一个监听进程 |
| 真实仓库入库 | 6 个真实仓库已入库，当前 4691 条向量 | 已关闭 |
| Agent 问答与检索质量 | 检索链路已用真实数据验证；真实模型受 DeepSeek 出网与 Ollama 服务卡住影响未全部验收 | 普通终端启动后端并恢复 Ollama 后按 docs/10 继续 |
| DeepSeek 出网 | 受限沙箱内后端无法访问 `api.deepseek.com`，需要普通终端启动；已实现 Ollama 自动保底 | 普通终端启动后端 |
| git_rag_agentic_demo | 缺少本地镜像且 GitHub 网络当前不可用 | 网络恢复后重新同步并入库 |
| 非仓库闲聊问题 | `general_chat` 输出越界校验已实现，天气/生活问题即使触发检索也会清空检索证据 | 已关闭 |
| 检索后重排 | 已按语义分与词面相关度融合重排，低相关证据先过滤 | 已关闭 |
| 检索后纠错 | 新增 validate 节点；模型可用时做 LLM 相关性校验，无证据时进入重试或明确未找到 | 已关闭 |
| LangGraph 多轮工具调用 | 检索为空或证据不足时支持放宽项目/文件类型并重试，最多 3 轮 | 已关闭 |
| project_overview / read_file 工具 | 已实现并通过测试 | 后续继续验证真实仓库 |
| 增量更新 | 已按文件 hash 实现增量，索引摘要与图片描述缓存已完成 | 已关闭 |
| 记忆 CRUD | API 与前端管理页面已完成 | 已关闭 |
| 前端组件测试 | 已覆盖登录、仓库、聊天、记忆、模型配置 | 继续补充边界场景 |
| 端到端测试 | 登录、入库已执行；图片检索、项目概述与记忆中文召回已用真实数据验证；真实模型问答待执行 | 按 docs/10 验收标准继续 |
| 显式项目跨项目污染 | 无结果重试会先放宽用户明确指定的项目，图片检索曾返回其他仓库 | 已修复：显式项目不跨项目兜底，图片/文档专用检索不降级类型 |
| 检索能力 | Chroma + ONNX 已接入，Vue 类项目级问题已改善；`general_chat`、重排、LLM 校验已补齐 | 已关闭 |

## 5. 交接启动方式

后端：

```bash
cd E:\mmmmmmmmmmmmmmmm\MyAgentic
uv sync
uv run python -m backend.app
```

必须在普通终端中启动，不能放在无法访问外网的沙箱里；启动前先确认 8000 端口没有旧后端进程。

前端：

```bash
cd E:\mmmmmmmmmmmmmmmm\MyAgentic\frontend
npm install
npm run dev
```

启动后必须使用 `http://127.0.0.1:5173` 访问前端；不要使用 `localhost`，否则 GitHub OAuth 的 state 会话仍会不匹配。

测试：

```bash
cd E:\mmmmmmmmmmmmmmmm\MyAgentic
uv run pytest
uv run ruff check backend
```

## 6. 关键配置

- .env：GitHub OAuth、会话密钥、加密密钥、模型配置。
- GITHUB_CALLBACK_URL：`http://127.0.0.1:8000/api/auth/callback`，必须与 GitHub OAuth App 一致。
- 前端 Vite 代理：`/api` 转发到 `http://127.0.0.1:8000`。
- 登录请求不走 Vite 代理，直接请求 `http://127.0.0.1:8000/api/auth/login`，保证 state 会话 cookie 与回调同 host。
- 数据目录：data/；向量目录：storage/chroma/。

## 7. 关键文件

| 文件 | 说明 |
|------|------|
| backend/app/main.py | FastAPI 入口 |
| backend/app/services/ | 业务服务层 |
| backend/app/api/ | API 路由 |
| frontend/src/views/ | 前端页面 |
| docs/04-长任务流.md | 阶段任务与验收 |
| docs/07-任务清单.md | 任务清单 |
| docs/09-发布清单.md | Git 上传前检查与提交步骤 |
| docs/10-验收标准.md | 分阶段验收标准与当前验证状态 |

## 8. 注意事项

- `.env` 包含真实 GitHub Client Secret，不要提交到 Git，也不要贴到日志或聊天记录中。
- Python 版本锁定 3.13；如引入新依赖需执行 `uv sync`。
- 当前使用 ONNX `bge-small-zh-v1.5`，向量维度 512；未配置模型时回退 Hash。
- 重索引时一次只处理一个仓库，embedding 按 32 条一批写入，避免内存溢出。
- Chroma 当前存储 4697 条向量；`git_rag_agentic_demo` 因缺少本地镜像未重建。
- 后端必须能访问 `https://github.com`；否则 OAuth 回调会在换 token 时连接失败。
- 当前工作点在阶段 10：登录与入库已验收；图片检索、项目概述与记忆中文召回已用真实数据验证；聊天历史工具/模式持久化、长期记忆中文召回与项目隔离、显式项目检索防跨项目污染已完成；真实模型问答受 DeepSeek 出网与 Ollama 服务状态影响待验收。
- 当前进度：登录、仓库列表、6 个仓库 Chroma 重建已验证；ONNX、Chroma、LangGraph、SSE 流式、URL 导入、UI 重做、检索纠错、模型保底已完成；下一步在普通终端启动后端并恢复 Ollama 后完成真实模型问答与浏览器端到端验收。
- 验收标准见 `docs/10-验收标准.md`。
